apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: astraguard
  labels:
    app: grafana
    component: visualization
spec:
  selector:
    app: grafana
    component: visualization
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
