apiVersion: v1
kind: Service
metadata:
  name: redis-exporter
  namespace: astraguard
  labels:
    app: redis-exporter
    component: monitoring
spec:
  selector:
    app: redis-exporter
    component: monitoring
  ports:
  - name: metrics
    port: 9121
    targetPort: 9121
    protocol: TCP
  type: ClusterIP
