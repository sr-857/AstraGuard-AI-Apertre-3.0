apiVersion: v1
kind: Secret
metadata:
  name: astraguard-secrets
  namespace: astraguard
type: Opaque
data:
  # Base64 encoded values - replace with actual secrets
  # Example: echo -n 'your-secret-value' | base64
  database-password: ""  # Fill with base64 encoded password
  redis-password: ""     # Fill with base64 encoded password
  grafana-admin-password: "YWRtaW4="  # 'admin' base64 encoded
  jwt-secret: ""         # Fill with base64 encoded JWT secret
