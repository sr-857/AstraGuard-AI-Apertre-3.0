# ============================================================================
# AstraGuard-AI Environment Configuration
# ============================================================================
# Copy this file to .env and update values for your environment
# cp .env.example .env

# ============================================================================
# Application Configuration
# ============================================================================
ENVIRONMENT=development
DEBUG=true

# ============================================================================
# Redis Configuration
# ============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379

# ============================================================================
# API Configuration
# ============================================================================
API_HOST=0.0.0.0
API_PORT=8000
STREAMLIT_PORT=8501

# ============================================================================
# Monitoring & Observability
# ============================================================================
LOG_LEVEL=DEBUG
GRAFANA_PASSWORD=admin
PROMETHEUS_RETENTION=7d

# ============================================================================
# Python Configuration
# ============================================================================
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# ============================================================================
# Optional: Azure Configuration (for production deployment)
# ============================================================================
# AZURE_RESOURCE_GROUP=astraguard-rg
# AZURE_CONTAINER_REGISTRY=astraguardregistry
# AZURE_STORAGE_ACCOUNT=astraguardstorage

# ============================================================================
# Optional: Machine Learning Configuration
# ============================================================================
# MODEL_PATH=/models
# BATCH_SIZE=32
# MAX_WORKERS=4

# ============================================================================
# Security Configuration (REQUIRED for metrics endpoint)
# ============================================================================
# Metrics endpoint authentication credentials
# ⚠️  SECURITY: These credentials protect the /metrics endpoint
# ⚠️  DO NOT use default values like "admin/admin" in production!
# ⚠️  Generate strong passwords: python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# Development (set these for local testing):
METRICS_USER=dev_user
METRICS_PASSWORD=dev_password_change_me

# Production (use strong, randomly-generated credentials):
# METRICS_USER=metrics_monitor
# METRICS_PASSWORD=<generate-with-secrets.token_urlsafe(32)>
#
# Security Best Practices:
# 1. Use passwords with 20+ random characters
# 2. Never commit real credentials to version control
# 3. Use environment-specific .env files (not in git)
# 4. Consider secrets management tools (Vault, AWS Secrets Manager)
# 5. Rotate credentials regularly (every 90 days minimum)

# ============================================================================
# Optional: Additional Security Configuration (for production)
# ============================================================================
# API_KEY=your-api-key-here
# JWT_SECRET=your-jwt-secret-here
# CORS_ORIGINS=https://yourdomain.com
