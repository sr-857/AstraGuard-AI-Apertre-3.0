# ===== LIGHTWEIGHT CI DEPENDENCIES (optimized for GitHub Actions) =====
# This file reduces install time and disk pressure by excluding heavy optional packages
# suitable for full CI/testing without GPU. Use this instead of requirements.txt in CI workflows.

# Core data processing
numpy==1.26.4
pandas>=2.2.0  # 2.1.4 doesn't support Python 3.13; use 2.2+ for 3.13 compatibility
scikit-learn==1.5.2

# Web framework & async
fastapi==0.128.0
uvicorn[standard]==0.30.0
aiohttp>=3.11.0,<4.0
pydantic>=2.10.0
httpx==0.25.0

# ML/AI (CPU-only torch - MUST use --index-url https://download.pytorch.org/whl/cpu at install time)
torch>=2.2.0,<3.0
sentence-transformers==5.1.2

# Observability & monitoring
prometheus-client==0.21.0

# Infrastructure & coordination
redis==5.0.1
aiosqlite>=0.19.0  # Async SQLite support for contact form
aiofiles>=23.0.0  # Async file operations for contact form

# Utilities
python-dateutil==2.8.2
pytz==2024.1
protobuf>=4.25.8,<5.0.0
loguru==0.7.2
lz4==4.0.2

# Test dependencies
pytest==8.3.2
pytest-cov==7.0.0
pytest-mock==3.15.1
pytest-timeout==2.1.0
pytest-asyncio==0.24.0
jsonschema==4.22.0
