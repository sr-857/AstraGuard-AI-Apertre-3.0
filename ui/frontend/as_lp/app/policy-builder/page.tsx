import { VisualPolicyBuilder } from "@/components/visual-policy-builder"

export default function PolicyBuilderPage() {
  return <VisualPolicyBuilder />
}

export const metadata = {
  title: "Visual Policy Builder | AstraGuard AI",
  description: "Create and manage recovery policies with drag-and-drop interface",
}